bash script:
make the container name as a recieved parameter ($1)
try to shift from global variables and use return in functions
formaling the log in a way that another program can parse (json like) + add date


add (logfile) weird handler\timeout file so the script wont overflow the system